# Simplified Bazel build file for scripts directory
# No longer needs the custom Python toolchain with modern Bazel

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "setup_scripts",
    srcs = [
        "regen_protos.sh",
        "setup_bazel.sh",
    ],
)